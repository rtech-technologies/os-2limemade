#include <include/rsl.h>
#include <include/vfs.h>
#include <stdbool.h>

void rsl_dispatch_command(char* line, void** curdir_ptr, bool* is_safe_ptr) {
    (void)line; (void)curdir_ptr; (void)is_safe_ptr;
    /* Dummy for now */
}
